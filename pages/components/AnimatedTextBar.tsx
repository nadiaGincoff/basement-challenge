const AnimatedTextBar: React.FC = () => {
  return (
    <section className="mobile:max-w-full overflow-hidden border-t-1 border-b-1 box-border">
      <div className="overflow-left-right box-content">
        <p className="sm:text-2xl md:text-5xl m-1">
          A man can’t have enough base­ment swag  —  A man can’t have enough base­ment swag  — A man
          can’t have enough base­ment swag  — A man can’t have enough base­ment swag
        </p>
      </div>
    </section>
  );
};

export default AnimatedTextBar;
